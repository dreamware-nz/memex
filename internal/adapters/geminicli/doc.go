// Package geminicli is the Gemini CLI platform adapter — patcher for
// ~/.gemini/settings.json. Hook keys live at the top level using Gemini's
// names (BeforeTool, AfterTool, PreCompress, SessionStart).
package geminicli
